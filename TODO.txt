TODO

MACHINE LEARNING
- implemet direct control and reading of parameters via osc
- create midi and osc datasets
- create algorithms to find settings with largest ratio training_accuracy:test_accuracy
- make the algorith of higher order. eg: it calculates the next message based on the previous 10 messages
- create new artificial features like last note on, last note off, last control number
- a model that instead of generating each message parameter individualy, generates a comple message
by taking the input message, find the most similar message from the set and outputs the next message
the next message from the set (more accuracy but less variation)

1.0.1

X.X.X
- implement textual
- separate supercollider output from command line
- implement commands with multiple words
- learn neovim
- learn openFrameworks
- record buses
- create agent
- find better logic for multiword commands

DONE
X create main repl app
X add load instrument capabilities
X add posibility to disconnect partially or completely from interface
X add unload instrument capabilites
    X fix destroy command
X add harpa as a module (git)
X load python code for instrument instances from repl
X implement rest of osc commands
X clean up code
X 1.0.0
X implement midi recording and playback
    X stop midi recording on exit
